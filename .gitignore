# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env overrides. The Lovable-managed .env is committed on purpose:
# it holds only the publishable Supabase keys, which ship in the client
# bundle anyway. Anything genuinely secret goes in a .local file, and
# those must never be committed.
.env.local
.env.*.local
